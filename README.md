# APU Profiler (Capstone)
Aim: a lib that support unified profiler on multiple models. (should be verified on VGGT and SmolVLM).

1. can `pip install`
2. can `import` to generate profiles
3. has cli or script to run. e.g. `python scripts/run_benchmark.py --model smolvlm --backend torch --device cuda`
